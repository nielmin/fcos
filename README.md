# Fedora CoreOS VPS Configuration

## Features
- Containers using Podman Quadlet
    - Custom Caddy build with Cloudflare DNS support
    - NetBird Mesh VPN
        - Setup key stored in `podman secret`
